{
  "$comment": "Brainforge is packaged as a Claude plugin from day one (DESIGN.md §6B). The BUILDER layer (commands/) is Claude-first; the brain it scaffolds (scaffold/) is tool-agnostic markdown+JSON readable by any LLM.",
  "name": "brainforge",
  "version": "0.3.0",
  "description": "Build and operate a product-org brain — a tool-agnostic git repo of authored truth + synced facts that any LLM can read.",
  "author": { "name": "jrpease" },
  "keywords": ["brain", "context", "source-of-truth", "canon", "sync", "design-system"],
  "scaffold": "scaffold/",
  "license": "MIT",
  "repository": "https://github.com/jrpease/brainforge",
  "runtime": {
    "$comment": "The seam. Files re-emittable on /upgrade (commands/upgrade.md) — the bumpable behavior layer, like react-scripts. Everything else in scaffold/ is emitted once and owned by the brain. Emitted brains carry .brainforge/runtime-manifest.json (this version + sha256 per bump file, hashed as emitted) — the baseline /upgrade compares against. Corollary: touching a bump path here means bumping version above, or the change is invisible to upgrades.",
    "bump": [
      "pipeline/**",
      "authoring/README.md",
      "setup/README.md",
      ".claude/commands/**",
      ".claude/settings.json",
      ".brainforge/README.md"
    ],
    "once": [
      "README.md", "CLAUDE.md", "CONTRIBUTING.md",
      "sources.json", ".sync-state.json",
      ".gitignore", ".gitattributes", ".env.example",
      "templates/**",
      "authoring/templates/**",
      "context/canon/README.md", "context/derived/README.md"
    ]
  },
  "publish": {
    "$comment": "The distribution seam (Phase 4). include = allowlist globs copied verbatim; anything unmatched is private by default. map = authored public variants renamed at emit (never scrub the private originals). Corollary: touching a published path means re-running /publish before the next release. The audit leak-denylist lives in publish-audit.json at repo root — private by design (matched by no include glob), so the denylist never ships with the set it polices.",
    "repo": "jrpease/brainforge",
    "include": [
      ".claude-plugin/**",
      "commands/**",
      "scaffold/**",
      "docs/proofs/**",
      "LICENSE"
    ],
    "map": {
      "README.public.md": "README.md",
      "DESIGN.public.md": "DESIGN.md",
      "ROADMAP.public.md": "ROADMAP.md",
      "CONTRIBUTING.md": "CONTRIBUTING.md",
      "docs/walkthroughs/shopify-add-adapter.md": "docs/walkthroughs/shopify-add-adapter.md"
    }
  }
}
